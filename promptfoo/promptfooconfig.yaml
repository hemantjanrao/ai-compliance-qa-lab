# promptfoo regression suite — config-driven eval
# Regenerate tests: python scripts/generate_promptfoo_tests.py
# Run: make promptfoo-eval  OR  pytest eval/test_promptfoo.py -v -m eval
description: "EU AI Act QA - full golden-set prompt regression (v1 vs v2)"

prompts:
  - file://prompt-v1.txt
  - file://prompt-v2.txt

providers:
  - anthropic:messages:claude-haiku-4-5-20251001
  - openai:chat:gpt-4o-mini

defaultTest:
  vars:
    context: "(Prompt-layer eval — no live retrieval; tests prompt wording and safety rules.)"

tests: file://tests.generated.yaml
